# 心情树洞 · 运维手册（安全 & 长期稳定运行）

本轮升级已内置的加固（改代码即生效，无需你操作）：

- **安全响应头**：内容嗅探防护、Referrer 策略、点击劫持防护（X-Frame-Options=SAMEORIGIN）。
- **Cookie 安全**：会话 cookie 设为 HttpOnly，SameSite=Lax。
- **接口限流**（防刷、防烧 API 额度）：AI 树洞 60 秒最多 20 条；语音合成 60 秒 30 次；投稿 5 分钟 10 次；注册/登录 5 分钟 8 次。阈值可在 `.env` 调。
- **上传限制**：音频 ≤20MB 且仅限音频格式；头像 ≤5MB 且仅限图片格式；请求体 ≤5MB。
- **错误日志**：写入 `logs/site.log`（滚动，单文件 5MB、留 5 份），方便排查崩溃。

---

## 一、上线前：`.env` 生产配置（必做）

编辑 `.env`，设置：

```
DJANGO_DEBUG=False
DJANGO_SECRET_KEY=<换成强随机串，生成方法见下>
DJANGO_ALLOWED_HOSTS=sc1.dpfrp.top
CSRF_TRUSTED_ORIGINS=http://sc1.dpfrp.top:12345
```

生成强 SECRET_KEY（cmd 里，激活 venv 后）：
```bat
python -c "import secrets;print(secrets.token_urlsafe(64))"
```
把输出粘到 `DJANGO_SECRET_KEY=`。

> **DEBUG=False 是最重要的一项**：DEBUG=True 时，任何报错页会把你的代码、路径、配置暴露给访客，是最大的安全隐患。
> 改 DEBUG=False 后记得 `python manage.py collectstatic --noinput`（whitenoise 托管静态文件）。

可选（限流阈值等）：
```
RL_CHAT_N=20        # 树洞每分钟条数
RL_LOGIN_N=8        # 登录/注册每5分钟次数
MAX_AUDIO_MB=20     # 音频上传上限
MAX_IMAGE_MB=5      # 头像上限
```

以后若上了 HTTPS，再加 `ENABLE_HTTPS=True` 和 `SECURE_COOKIES=True`（没有 HTTPS 时**千万别开**，否则会把自己锁在门外）。

---

## 二、数据库自动备份（强烈建议）

SQLite 是单个文件，最怕突然断电/进程崩溃导致文件损坏。已内置备份脚本。

**手动备份一次**：双击 `备份数据.bat`（或 `python backup_db.py`）。
备份存到 `backups/`，含数据库和媒体文件，带时间戳，自动保留最近 30 份。

**设为每天自动备份**（Windows 任务计划程序）：
1. 开始菜单搜「任务计划程序」→ 打开。
2. 右侧「创建基本任务」→ 名称填「心情树洞备份」→ 下一步。
3. 触发器选「每天」，时间选个没人用的时段（如凌晨 3:00）。
4. 操作选「启动程序」，程序/脚本填：
   ```
   D:\moodsite\moodsite1.1.1\备份数据.bat
   ```
   （路径按你的实际目录）
5. 完成。以后每天自动备份，硬盘出问题也能从 `backups/` 恢复。

**恢复**：停掉服务器，把 `backups/` 里某个 `db_时间戳.sqlite3` 复制回项目根、改名为 `db.sqlite3` 即可。

---

## 三、开机自启 + 崩溃自动重启（nssm，强烈建议）

`run_server.bat` 挂着黑窗口的方式：窗口被关、电脑重启、程序崩溃后就停了。用 **nssm** 把它注册成 Windows 服务，能开机自启、崩溃自动拉起、后台无窗口运行。

1. 下载 nssm：https://nssm.cc/download （解压，用里面 win64/nssm.exe）。
2. 以**管理员身份**打开 cmd，进到 nssm.exe 所在目录，运行：
   ```bat
   nssm install MoodSite
   ```
3. 弹出的窗口里填：
   - **Path**：你 venv 里的 python，例如
     `D:\moodsite\moodsite1.1.1\venv\Scripts\python.exe`
   - **Startup directory**：`D:\moodsite\moodsite1.1.1`
   - **Arguments**：
     `-m waitress --listen=0.0.0.0:8000 moodsite.wsgi:application`
   - 切到 **I/O** 标签，可把 Output/Error 指到 `D:\moodsite\moodsite1.1.1\logs\service.log` 便于查日志。
   - 切到 **Exit actions**，确认是「Restart」（崩溃自动重启，默认就是）。
4. 点 Install service。然后启动：
   ```bat
   nssm start MoodSite
   ```
5. 以后它会开机自启、后台运行。常用命令：
   ```bat
   nssm stop MoodSite       :: 停止
   nssm restart MoodSite    :: 重启（更新代码后用）
   nssm remove MoodSite     :: 卸载服务
   ```

> 用了 nssm 服务后，就**不要**再双击 `run_server.bat`，避免两个进程抢 8000 端口。

---

## 四、数据库选型建议（按需）

**现在用的 SQLite 够用吗？** —— 对个人/小范围（几十到几百用户、并发不高）**完全够用**，配合上面的每日备份就行，不用折腾。

**什么时候该换 PostgreSQL？**
- 用户量上来了（比如上千日活），或经常有「多人同时写入」——SQLite 同一时刻只允许一个写操作，并发写会出现 `database is locked`。
- 需要更强的数据完整性和并发能力。

换 PostgreSQL 是架构改动（要装数据库、改 `DATABASES` 配置、迁移数据），不复杂但有几步。**建议：先用 SQLite + 每日备份跑着，等真遇到锁库或用户明显变多，再来找我帮你迁移，别提前折腾。**

---

## 五、日常巡检清单

- 每隔一阵看看 `logs/site.log` 有没有反复出现的 ERROR。
- 确认 `backups/` 里每天有新备份（说明定时任务在跑）。
- DeepSeek 余额别用完（树洞和 AI 审核都靠它）。
- 系统/依赖偶尔更新：`pip install -U -r requirements.txt`（更新后测一下再上线）。
- 心理援助热线号码定期核对是否仍有效。
